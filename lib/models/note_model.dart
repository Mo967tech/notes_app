class NoteModel {
  final String title, content, date;
  final int color;
  
  const NoteModel({
    required this.title,
    required this.content,
    required this.date,
    required this.color,
  });
}
